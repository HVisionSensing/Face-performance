#ifndef INITREGISTER_HH_INCLUDED
#define INITREGISTER_HH_INCLUDED
/*
#include "InitRegisterMesh.h"
#include <TriMesh.h>
#include <TriMesh_algo.h>
#include <fstream>
class InitRegister
{
public:
	InitRegister(trimesh::TriMesh* pointCloud, std::string& meshIndexFilename, std::string& depthIndexFilename);
	~InitRegister();
	//void evalfunc(int N, double* x, double *prev_x, double* f, double* g);
	//void newiteration(int iter, int call_iter, double *x, double* f, double *g,  double* gnorm);
	//(void Optimize_by_HLBFGS(int N, double *init_x, int num_iter, int M, int T);
	//void Optimize_by_LM(int N, double *init_x, int num_iter, int M, int T);
	void register_mesh(int num_iter, trimesh::TriMesh * tmesh);
public:
	trimesh::TriMesh * pointCloud;
	InitRegisterMesh * initRM;
	std::ofstream of;
};*/
#endif